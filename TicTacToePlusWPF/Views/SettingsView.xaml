<UserControl x:Class="TicTacToePlusWPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TicTacToePlusWPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White">

    <StackPanel Margin="40" VerticalAlignment="Center" HorizontalAlignment="Center">
        <TextBlock Text="Game Settings" FontSize="28" FontWeight="Bold" Margin="0,0,0,30"/>

        <!-- Input grid for basic settings -->
        <Grid Margin="0,0,0,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Grid Rows:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
            <TextBox Text="{Binding GridRows}" Grid.Row="0" Grid.Column="1" Width="60"/>

            <TextBlock Text="Grid Columns:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,0,0"/>
            <TextBox Text="{Binding GridColumns}" Grid.Row="1" Grid.Column="1" Width="60" Margin="0,10,0,0"/>

            <TextBlock Text="Win Condition:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,0,0"/>
            <TextBox Text="{Binding WinCondition}" Grid.Row="2" Grid.Column="1" Width="60" Margin="0,10,0,0"/>

            <TextBlock Text="Player Count:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,10,0,0"/>
            <TextBox Text="{Binding PlayerCount}" Grid.Row="3" Grid.Column="1" Width="60" Margin="0,10,0,0"/>
        </Grid>

        <!-- Player Symbols -->
        <TextBlock Text="Player Symbols:" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <ItemsControl ItemsSource="{Binding PlayerSymbols}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <TextBox Text="{Binding Symbol, UpdateSourceTrigger=PropertyChanged}" 
                             Width="40" Height="40" Margin="5"
                             FontSize="16" FontWeight="Bold" TextAlignment="Center"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <!-- Save Button -->
        <Button Content="Save Settings" 
                Command="{Binding SaveSettingsCommand}" 
                Margin="0,30,0,0" Padding="10,5"
                Width="150" HorizontalAlignment="Center"/>
    </StackPanel>
</UserControl>
